# Foro - Clase 13

Hoy integramos frontend y backend real (mock en memoria):

- frontend Angular consume endpoints NestJS
- login real con `POST /auth/login`
- listado de conversaciones y mensajes por HTTP
- envio de mensajes con respuesta mock del asistente
- create/archive/activate de conversaciones

URLs usadas en clase:
- Frontend: `http://localhost:5300`
- Backend Swagger: `http://localhost:5001/api`
