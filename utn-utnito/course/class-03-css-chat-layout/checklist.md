# Checklist - Clase 03

## Español

Validar al final de la práctica:

- `login.html` muestra validaciones del navegador (`required`, `email`, `number`, `minlength`, `maxlength`, `pattern`).
- Quedó clara la diferencia entre link y button.
- `chat.html` tiene bloques de interfaz:
  - sidebar
  - chat-main
  - messages
  - composer
- `chat.css` está enlazado correctamente en `chat.html`.
- Layout final usa Flexbox:
  - sidebar fija
  - chat-main flexible
- Se visualizan mensajes en izquierda/derecha.
- Link `Log out` navega a `login.html`.
- La clase deja listo:
  - `chat.html`
  - `chat.css`

---

## English

Validate at the end of practice:

- `login.html` shows browser-native validation (`required`, `email`, `number`, `minlength`, `maxlength`, `pattern`).
- Difference between link and button is clear.
- `chat.html` contains interface blocks:
  - sidebar
  - chat-main
  - messages
  - composer
- `chat.css` is correctly linked in `chat.html`.
- Final layout uses Flexbox:
  - fixed sidebar
  - flexible chat-main
- Left/right message alignment is visible.
- `Log out` link goes to `login.html`.
- Class leaves ready:
  - `chat.html`
  - `chat.css`
