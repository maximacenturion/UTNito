# Step 3 - CSS intro + basic selectors

## Español

### Objetivo
Conectar `chat.css` y aplicar estilo base con selectores simples.

### Archivos a copiar
- `chat.html`
- `chat.css`

Copiar desde `steps/step-3` hacia `c03-html-start`.

### Conceptos a explicar
- CSS: presentación visual.
- Conexión HTML/CSS:
  - `<link rel="stylesheet" href="./chat.css" />`
- Sintaxis:
  - `selector { propiedad: valor; }`
- Selectores por elemento, clase e id.

## English

### Objective
Connect `chat.css` and apply base styling with simple selectors.

### Files to copy
- `chat.html`
- `chat.css`

Copy from `steps/step-3` into `c03-html-start`.
