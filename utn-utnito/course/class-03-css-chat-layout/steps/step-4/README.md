# Step 4 - Box model demo (isolated)

## Español

### Objetivo
Explicar box model en un ejemplo separado del chat para que el concepto sea claro.

### Archivos a copiar
- `box-model-demo.html`
- `box-model-demo.css`

Copiar desde `steps/step-4` hacia `c03-html-start`.

### Qué abrir
- Abrir `box-model-demo.html`.

### Qué experimentar
- Cambiar `padding`.
- Cambiar `border`.
- Cambiar `margin-bottom` en `.demo-box_one`.
- Cambiar `margin-top` en `.demo-box_two`.
- Observar cómo cambia la separación entre ambas cajas.

## English

### Objective
Explain box model using a separate example so the concept is clear before the real chat layout.

### Files to copy
- `box-model-demo.html`
- `box-model-demo.css`
