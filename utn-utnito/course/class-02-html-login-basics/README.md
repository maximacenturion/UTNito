# Clase 02 - HTML Básico + Login

## Español

### Objetivo
Construir una pantalla de login estática con HTML y CSS, usando una estructura simple y semántica.

### Duración sugerida
60 minutos (6 bloques de 10 minutos)

### Estructura de la clase
- `slides/`: guía para armar el PPT de la clase.
- `c02-html-start`: proyecto inicial para arrancar la práctica.
- `steps/step-1`: estructura semántica del login.
- `steps/step-2`: formulario de login + navegación a `chat.html`.
- `steps/step-3`: estilos CSS base para dejar el login presentable.
- `c02-html-end`: resultado final esperado al terminar la clase.
- `checklist.md`: validación de cierre para alumnos.
- `troubleshooting.md`: errores comunes y resolución rápida.
- `resources/`: recursos auxiliares para foro y explicación.

### Herramientas de hoy
- Visual Studio Code
- Navegador web (Chrome, Edge, Firefox o Safari)
- Git (solo para sincronizar repositorio)

### Flujo de la práctica
1. Clonar repositorio del curso:
   - `git clone https://github.com/matmatys/programacion_III_UTN.git`
2. Entrar a la carpeta de la clase:
   - `cd programacion_III_UTN/utn-utnito/course/class-02-html-login-basics/c02-html-start`
3. Abrir `c02-html-start` en VS Code.
4. Abrir `login.html` con doble click para ver el estado inicial.
5. Copiar archivos de `steps/step-1` sobre `c02-html-start` y validar.
6. Copiar archivos de `steps/step-2` sobre `c02-html-start` y validar.
7. Copiar archivos de `steps/step-3` sobre `c02-html-start` y validar.

### Criterio de clase aprobada
El alumno logra una pantalla de login funcional en HTML/CSS y al presionar `Login` navega a un `chat.html` placeholder para continuar en la Clase 03.

---

## English

### Objective
Build a static login screen with HTML and CSS using a simple semantic structure.

### Suggested Duration
60 minutes (6 blocks of 10 minutes)

### Class Structure
- `slides/`: guide to prepare class slides.
- `c02-html-start`: initial project used to start the practice.
- `steps/step-1`: semantic login structure.
- `steps/step-2`: login form + navigation to `chat.html`.
- `steps/step-3`: base CSS styles for a presentable login screen.
- `c02-html-end`: expected final result at the end of class.
- `checklist.md`: closing validation checklist for students.
- `troubleshooting.md`: common errors and quick fixes.
- `resources/`: support resources for forum and explanation.

### Today's Tools
- Visual Studio Code
- Web browser (Chrome, Edge, Firefox, or Safari)
- Git (only to sync repository)

### Practice Flow
1. Clone the course repository:
   - `git clone https://github.com/matmatys/programacion_III_UTN.git`
2. Go to the class folder:
   - `cd programacion_III_UTN/utn-utnito/course/class-02-html-login-basics/c02-html-start`
3. Open `c02-html-start` in VS Code.
4. Open `login.html` with double click to see the initial state.
5. Copy files from `steps/step-1` into `c02-html-start` and validate.
6. Copy files from `steps/step-2` into `c02-html-start` and validate.
7. Copy files from `steps/step-3` into `c02-html-start` and validate.

### Class Completion Criteria
The student delivers a functional HTML/CSS login screen and can navigate to a `chat.html` placeholder by pressing `Login`, preparing for Class 03.
