# Checklist - Clase 02

- [ ] Abrí `c02-html-start` en VS Code.
- [ ] Abrí `login.html` en el navegador desde archivo local.
- [ ] Apliqué `steps/step-1` y entendí la estructura semántica (`main`, `section`, `header`, `footer`).
- [ ] Apliqué `steps/step-2` y completé el formulario (`form`, `label`, `input`, `button`).
- [ ] Apliqué `steps/step-3` y visualicé estilos CSS básicos.
- [ ] Verifiqué que el botón `Login` lleva a `chat.html`.
- [ ] Comparé mi resultado con `c02-html-end`.
