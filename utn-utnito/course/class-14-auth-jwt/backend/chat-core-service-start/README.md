# class-14 backend/chat-core-service-start

NestJS backend start state for class 14.

This is the initial backend before JWT is added in Step 1 and Step 2.

## Run

```bash
npm install
npm run start:dev
```

- API base: `http://localhost:5001`
- Swagger: `http://localhost:5001/api`
